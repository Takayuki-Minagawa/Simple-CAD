# Structural Web CAD — 精度向上のための追加機能提案

> 4つの専門観点（①作図・幾何精度 ②構造設計ドメイン精度 ③データ整合性・入出力精度 ④操作精度・UX）から、サブエージェントによる現状コード調査に基づいて抽出した追加機能を整理したもの。
> 調査対象: `src/domain/geometry`, `src/domain/structural`, `src/domain/validation`, `src/domain/integration`, `src/domain/import|export`, `src/features/editor2d`, `src/features/viewer3d`, `src/features/project`, `src/schemas/project.schema.json`

---

## 0. 調査で判明した「既存バグ／死に機能」（最優先・追加機能の前提）

精度向上の前に、**現状仕様の不整合**として確認された以下を先に修正することを強く推奨する。

| # | 内容 | 該当箇所 | 影響 |
|---|------|----------|------|
| B1 | **交点スナップ(intersection)が死に機能** — デフォルト有効なのに `findSnap()` に実装が無い | `editorStore.ts`（既定ON） / `snap.ts`（分岐欠落） | 節点をスナップで合わせられない |
| B2 | **編集ハンドルのドラッグがグリッドにしかスナップしない** — 作図時の端点・中点スナップを無視 | `SelectionHandles.tsx` `snapDragPoint` | 編集で接合がずれる |
| B3 | **構造計算JSONの節点が完全一致でしか共有されない** — `${x}:${y}:${z}` キー、1mmのズレで節点分離 | `structuralAnalysisJson.ts` `ensureNode` | 連続梁・フレームが解析上切れる |
| B4 | **DXF出力に `$INSUNITS` が無い** — 受信CADが単位を誤認しスケールずれ | `dxfExport.ts` | 1000倍/25.4倍の事故 |
| B5 | **回転(member.rotation)が DXF/IFC 出力で反映されない** — 往復で回転消失 | `dxfExport.ts`, `ifc/export.ts` | 形状の再現性喪失 |
| B6 | **構造計算JSON取込で節点欠落部材を黙って捨てる** | `structuralAnalysisJson.ts` | 取込件数が無言で減る |
| B7 | **エクスポート時の断面欠落が暗黙のマジックナンバーに化ける**（`?? 600` 等）警告なし | `ifc/export.ts`, `structuralAnalysisJson.ts` | 誤断面で出力されても気づけない |

---

## 1. 作図・幾何精度（Geometry / Snap / Coordinate）

### 1-1. 交点スナップの実装（intersection snap）★最優先
- **課題**: B1。最も基本的なスナップが機能せず、ラーメン構造の格点を一致させられない。
- **方針**: `snap.ts` に線分交差 `segmentIntersection(a,b,c,d)`（行列式・平行除外・`t,u∈[0,1]`）を新設。`SnapCandidate.edges` の近傍ペアのみ評価（バウンディングボックスで O(n²) を抑制）。延長線上の見かけ交点はオプション。
- **難易度**: 中 / **効果**: 大

### 1-2. 編集ハンドルへのフルスナップ統合
- **課題**: B2。作図と編集でスナップ挙動が乖離。
- **方針**: `getSnapPos` 相当を共有関数化し、`SelectionHandles.tsx` のドラッグを `findSnap` 経由に（ドラッグ中部材は自己スナップ除外）。`editableHandles.ts` の共有端点マージと連動。
- **難易度**: 中 / **効果**: 大

### 1-3. 座標量子化ユーティリティ + EPSILON 一元化
- **課題**: 極・相対・回転変換の浮動小数誤差（`100·cos30°=86.6025…`）が正本に蓄積。等価判定の許容誤差が散在。
- **方針**: `geometry/precision.ts`（新規）に `GEOM_EPSILON`, `quantize`, `quantizePoint2D`, `equals2D` を集約。極/相対計算結果を量子化（既定0.001mm）。散在マジックナンバーを統一。B3 のノードキーにも再利用。
- **難易度**: 小 / **効果**: 中（全機能の土台）

### 1-4. 極トラッキング（Polar Tracking）+ トラッキングHUD
- **課題**: 角度拘束が「Shiftで45°丸め」のみ。任意角度・距離/角度のリアルタイム表示・基準線が無い。
- **方針**: `editorStore` に `polarTrackingEnabled / polarAngleStep / polarBaseAngle`。`angleConstraint.constrainPointToAngle`（実装済）にステップを渡し、`DrawPreview` にトラッキング線とHUDラベル。既存点方向を基準にした直交トラッキングも。
- **難易度**: 中 / **効果**: 中〜大
- ※ 観点④ #3 と同一テーマ。UI（点線ガイド・`F8/F10`トグル）と合わせて実装。

### 1-5. スナップ優先度の重み付け + 種別マーカー
- **課題**: `findSnap` が純距離順で、近い中点が遠い端点に勝つ。種別が見た目で区別できず誤スナップ。
- **方針**: 「優先度バケット」方式（端点/交点を最優先層、同層内のみ距離比較）。種別ごとのマーカー形状（端点=□/中点=△/交点=×/垂線=⊥/最近傍=○）を描き分け（`SnapResult.type` は既存）。
- **難易度**: 小〜中 / **効果**: 中
- ※ 観点④ #4 と統合。

### 1-6. 作図線（construction line / xline）へのスナップ
- **課題**: 補助線を引けても線上・交点がスナップ候補に入らず「見えるだけ」。
- **方針**: `buildSnapCandidatesFromConstructionLines` を追加。無限直線を近傍クリップした擬似エッジ化し、nearest/perpendicular/intersection の対象に。
- **難易度**: 小〜中 / **効果**: 中

### 1-7. 寸法駆動の座標入力（距離ロック / Dynamic Input）
- **課題**: 座標入力がテキストバー submit のみ。方向はマウス・距離は数値固定の「距離ロック」が無い。
- **方針**: パース済の `@distance` をキーボード即時入力に昇格。Tab/数値キーで距離・角度フィールドにフォーカスし「方向=マウス, 距離=入力値」で確定。
- **難易度**: 中 / **効果**: 中
- ※ 観点④ #1（動的入力）と同一。統合実装。

### 1-8. 不正ジオメトリ検証（重複点・ゼロ長・自己交差）
- **課題**: ゼロ長梁/壁、面積0・自己交差スラブが正本に混入しうる。
- **方針**: `measurement.ts` に `isDegenerate / isSimplePolygon / hasDuplicateVertices`。確定時に検証し不正なら拒否＋トースト。一括監査にも展開。
- **難易度**: 中 / **効果**: 中
- ※ 観点③ #4 と統合（検証パイプラインへ）。

---

## 2. 構造設計ドメイン精度（Structural Correctness）

### 2-1. 通り芯参照システム（グリッド基準配置）★核心
- **課題**: 部材は絶対座標のみで通り芯(`Grid`)を参照しない。スパン変更時に部材が取り残され、寸法と実部材がズレる。
- **方針**: `MemberBase` に `gridRef?: { startGrid?:[string,string]; endGrid?:[string,string] }`。通芯交点→座標解決の純関数、`Grid.position` 変更時の部材再計算、通芯交点スナップ、通芯編集UI（スパン直接入力）を追加。
- **難易度**: 大 / **効果**: 大（実務CADとの差を最も縮める）

### 2-2. 材料物性マスタの拡張（Fc・ヤング係数・単位体積重量・許容応力度）
- **課題**: `Material` が `type` のみ。強度・剛性・自重が無く応力/たわみ計算が一切できない。
- **方針**: 判別ユニオン化（concrete: Fc/Ec/単位重量、steel: F値/Fy/Es、wood: 基準強度/含水）。スキーマ・`MaterialsSection`/`masterDataFields` に入力欄、プリセット（FC21–30/SN400–490）を `masterDataHelpers`。`structuralAnalysisJson` に自動伝播。
- **難易度**: 中 / **効果**: 大（構造計算連携の実用化）

### 2-3. 断面ライブラリ拡充（鉄骨H形鋼・配筋・かぶり）+ 妥当性チェック
- **課題**: RC矩形4種のみ。鉄骨(H/箱/円)が表現不能、配筋・かぶりが無く非現実値も警告なし。
- **方針**: `Section` ユニオンに `s_column_h/s_beam_h/s_pipe/src_*` を追加。RCに `rebar?/cover?`。JIS H形鋼ライブラリを同梱。`validation` に断面妥当性バリデータ（最小せい/幅・かぶり下限）。IFCプロファイルもIプロファイル化。
- **難易度**: 中〜大 / **効果**: 大（鉄骨造対応・IFC精度）

### 2-4. 節点トレランス統合 + 接合整合チェック
- **課題**: B3。節点が完全一致でしか共有されず解析モデルが切れる。梁の柱接続・柱の連層が未検証。
- **方針**: `ensureNode` をトレランス丸め（量子化 or 近傍探索）に。接合整合バリデータ（梁端の柱/他梁端への近接、上下柱の平面一致、スラブ外周と梁/壁閉ループの一致）を追加し既存 `Issue[]` パネルへ。
- **難易度**: 中 / **効果**: 大（解析可能性を担保＝最重要の精度向上）

### 2-5. 階高・レベル整合管理（EL自動連鎖）
- **課題**: `Story.elevation` と `height` が独立で「下階EL+高さ=上階EL」を保証しない。矛盾ELが発生。
- **方針**: レベル整合バリデータ（隣接階EL連続性、柱垂直スパン=階高、スラブ/壁レベルの範囲内）。`StoriesSection` に「EL自動連鎖モード」。部材レベルを基準＋オフセット表現に拡張。
- **難易度**: 中 / **効果**: 中〜大

### 2-6. 部材軸線オフセット・偏心（取り合い・芯ずれ）
- **課題**: 芯線座標のみで偏心を表現できず、作図上の取り合いと解析芯線が一致しない。
- **方針**: `MemberBase` に `axisOffset?:{dx,dy}` と `faceAlign?:'center'|'left'|'right'`。描画・IFC `orientedPlacement`・3D・構造計算連携に反映。偏心が断面を超える異常を警告。
- **難易度**: 中 / **効果**: 中

### 2-7. 自重・面荷重の自動算定と荷重マスタ
- **課題**: 構造計算JSONに荷重情報が皆無。材料物性・断面が揃えば自重/固定/積載荷重を自動算定可能。
- **方針**: `loadCases?`、`SlabSection`/`Story` に `deadLoad?/liveLoad?`。自重算定純関数（断面積×単位重量×長さ/面積）。`structuralAnalysisJson` に `loads` 出力。荷重ケース編集UI。
- **難易度**: 中〜大（2-2/2-3 依存）/ **効果**: 中〜大

### 2-8. アソシエイティブ寸法 + 暗黙フォールバック警告
- **課題**: B7。寸法が部材と紐付かず、部材移動で実態と乖離。断面欠落が黙ってマジックナンバー化。
- **方針**: `Dimension` に `refMemberIds?/associative?` を追加し部材移動時に再計算。最低でも「寸法値≠実部材間距離」バリデータ。各エクスポータのフォールバックを `warning` 発行に。
- **難易度**: 小〜中 / **効果**: 中

---

## 3. データ整合性・入出力精度（Validation / Interop）

### 3-1. ラウンドトリップ検証ハーネス ★品質の土台
- **課題**: DXF/IFC/構造計算JSON の export→import で座標・寸法・断面・部材数の保存が未検証。特にIFC壁/スラブのz基準・回転に非対称あり。
- **方針**: `roundtrip.test.ts` 群（全部材種・回転・複数階）+ トレランス付き比較 `roundtripDiff.ts`。差分レポートで非対称バグを回帰捕捉。
- **難易度**: 中 / **効果**: 大（3-2/3-3/3-6/3-8 の正しさを継続保証）

### 3-2. 数値精度・丸めポリシーの一元化
- **課題**: B3 含む丸め誤差が散在。DXFは full-precision、`isRectangle` トレランスは50mm固定、断面のみ丸め座標は丸めず不統一。
- **方針**: `geometry/precision.ts`（1-3 と共通）に `EPS_COORD/EPS_ANGLE/snap/pointKey`。`ensureNode` を `pointKey` 量子化に。DXF出力を桁固定フォーマッタに。トレランスを注入式に。
- **難易度**: 中 / **効果**: 大

### 3-3. 単位スケール・座標系メタデータの入出力
- **課題**: B4。DXFは `$INSUNITS` 無し、import も単位推定なし。IFC import は単位を読まず常にmm前提（m単位IFCで1/1000）。
- **方針**: DXF出力に `$INSUNITS(4)/$MEASUREMENT/$EXTMIN/$EXTMAX`。`dxfParser` にHEADER解析、不在時はバウンディングボックスで単位推定＋警告。IFC import に `IFCUNITASSIGNMENT/IFCSIUNIT/IFCCONVERSIONBASEDUNIT` 解析（`ifc/units.ts` 新設）。
- **難易度**: 中（IFC側やや大）/ **効果**: 大

### 3-4. 範囲・有限性・退化の幾何検証ルール追加
- **課題**: `geometryValidator` は長さ0と頂点数のみ。自己交差/共線退化スラブ、寸法offset異常、story EL重複/逆順、grid position重複、極小/極大部材長を検出しない。
- **方針**: `geometryValidator` を拡張（有限性・共線退化・自己交差・極小極大長）。`topologyValidator.ts`（新規）でEL単調性・grid重複。IFCの0長フォールバックを「スキップ+warning」に。
- **難易度**: 小〜中 / **効果**: 中（1-8 と統合）

### 3-5. 参照整合性の網羅化
- **課題**: `referenceValidator` が主要参照のみ。group→member、viewport→view/sheet、opening→member型、**断面種別と部材種別の整合**（column が rc_slab を参照しても通る）、一部ID重複が未検証。
- **方針**: 上記参照と section.kind↔member.type マッピング検証を追加。`externalRefs.data` のネスト ProjectData を `validateProject` 再帰検証。
- **難易度**: 小 / **効果**: 中（即効・低コスト）

### 3-6. 構造計算JSONのJSON Schema化と双方向検証
- **課題**: 手書き検証で参照整合/enum が欠落。取込時に存在しないノード参照部材を黙って落とす（B6）。
- **方針**: `structuralAnalysis.schema.json`（新規、`$defs` を project schema と共有）でAjv2020検証。落とした部材は `warning` に積みサイレントドロップ廃止。
- **難易度**: 中 / **効果**: 中

### 3-7. schemaVersion マイグレーション基盤
- **課題**: `schemaVersion` は形式検証のみ。`additionalProperties:false` が厳格で旧JSONが将来弾かれる。
- **方針**: `domain/migration/` に `migrate(data)` をバージョン連鎖適用、`jsonImport` で検証前に挟む。物性/形鋼kind を version 連動で段階導入。往復テスト。
- **難易度**: 中 / **効果**: 中（後方互換の保証）

### 3-8. DXF/IFC ジオメトリ再現性の強化
- **課題**: B5。回転が往復で消失、IFCスラブz基準が非対称、DXFに高さ/階情報なし、`$EXTMIN/MAX` 未出力。
- **方針**: DXF/IFC export で `member.rotation` を placement/`refDirection` に反映、import で逆算復元。スラブz基準を export/import で一致（3-1 で担保）。DXFにバウンディングボックス出力。
- **難易度**: 中〜大 / **効果**: 大

---

## 4. 操作精度・UX・ヒューマンエラー防止

### 4-1. 動的入力（カーソル追従の数値入力）
- **課題**: 座標入力バーが画面下端固定で視線移動が大きい。長さ/角度を見ながらタイプできない。
- **方針**: カーソル右下に追従するHTMLオーバーレイ（`foreignObject` 等）に「長さ」「角度」フィールド、`Tab`で切替・ロック。既存 `parseCoordinate/injectCoordinate` に橋渡し。
- **難易度**: 中 / **効果**: 大（投資対効果高）/ ※1-7 と同一実装

### 4-2. リアルタイム寸法・角度プレビュー
- **課題**: 確定するまで長さ/角度が不明。目分量配置による寸法ミス。
- **方針**: `DrawPreview` に長さラベル（線分中点）・角度ラベル（始点付近）。スラブは各辺長・暫定面積・周長。ズーム非依存サイズ、`DimensionLayer` の反転手法を流用。
- **難易度**: 小〜中 / **効果**: 大（CAD基本機能・最優先実装候補）

### 4-3. 極トラッキング / 直交ガイド（UI側）
- **課題**: 角度拘束がShift必須・45°固定で視覚ガイドなし。水平/垂直・延長線に乗せにくい。
- **方針**: 設定可能な極角度（0/15/30/45/90°）、点線トラッキングガイド線＋「極:90°」ラベル、端点からの水平/垂直延長トラッキング。`EditorControls2D` トグル、`F8(ortho)/F10(polar)`。
- **難易度**: 中〜大 / **効果**: 大 / ※1-4 と統合

### 4-4. スナップ種別の視覚マーカー化
- **課題**: 全スナップが同じ橙十字で区別不能、誤スナップによる接続ミス。
- **方針**: `SnapResult.type` ごとにAutoCAD準拠マーカー（□△×⊥砂時計）＋種別名ツールチップ。
- **難易度**: 小 / **効果**: 中 / ※1-5 と統合

### 4-5. 座標入力の検証フィードバック
- **課題**: `parseCoordinate` が不正時 `null` で黙って無視され、原因が分からない。
- **方針**: 不正時に入力欄を赤枠化＋書式例表示。入力途中は解釈結果のゴースト点を `DrawPreview` にライブ表示。`coordinateInput` を理由付き Result 型に。
- **難易度**: 小〜中 / **効果**: 中

### 4-6. ステータスバーの精度・単位コンテキスト強化
- **課題**: 座標が `toFixed(0)` 固定、作図中の差分(dx,dy,距離,角度)が見えない。
- **方針**: 作図中は dx/dy/距離/角度、選択時は幅・高さ。精度桁数・単位(mm/m)を設定可能。スナップ確定時はスナップ点値を表示。グリッド間隔併記。
- **難易度**: 小 / **効果**: 中

### 4-7. 3D計測ツール（2点間距離・部材寸法プローブ）
- **課題**: 3Dに計測手段が皆無で、Z方向・階間の不整合を目視でしか確認できない。
- **方針**: `Viewer3D` に計測モード。メッシュ上の2点ヒットから3D距離・dx/dy/dz成分を `Html` ラベル表示。ホバーで部材長・断面名・所属階のツールチップ。端点優先スナップ。`ViewerToolbar` トグル。
- **難易度**: 中〜大 / **効果**: 中〜大

---

## 5. 推奨ロードマップ（横断統合）

重複テーマ（極トラッキング=1-4/4-3、動的入力=1-7/4-1、スナップ種別=1-5/4-4、節点トレランス=B3/2-4/3-2、ジオメトリ検証=1-8/3-4）は統合実装する前提。

### フェーズ1：既存バグ修正＋低コスト即効（精度の足場固め）
1. **B1/1-1 交点スナップ実装**
2. **B2/1-2 編集ハンドルのフルスナップ**
3. **1-3/3-2 量子化・丸めポリシー一元化**（`geometry/precision.ts`）
4. **B3/2-4 節点トレランス**（3 の `pointKey` を再利用）
5. **B7/2-8 フォールバック警告**、**3-5 参照整合の網羅化**、**3-4/1-8 幾何検証追加**

### フェーズ2：UX精度（作図体験の数値化）
6. **4-2 リアルタイム寸法プレビュー** → **4-4 スナップ種別マーカー** → **4-5 入力検証** → **4-6 ステータスバー強化**
7. **4-1/1-7 動的入力** → **1-4/4-3 極トラッキング**

### フェーズ3：構造ドメイン基盤
8. **2-2 材料物性** → **2-3 断面拡充** → **2-7 荷重自動算定**
9. **2-5 レベル整合** / **3-3 単位メタデータ** / **3-1 ラウンドトリップ検証**（以降の往復精度を恒久担保）

### フェーズ4：実務適合の核
10. **2-1 通り芯参照システム**、**2-6 偏心**、**3-8 DXF/IFC再現性**、**3-6 構造計算JSON Schema化**、**3-7 マイグレーション基盤**、**4-7 3D計測**

---

## 6. 新設が想定される主要ファイル
- `src/domain/geometry/precision.ts` — EPSILON/量子化/pointKey の一元管理（1-3, 3-2）
- `src/domain/validation/topologyValidator.ts` — EL単調性・grid重複・接合整合（2-4, 2-5, 3-4）
- `src/domain/validation/roundtripDiff.ts` + `__tests__/roundtrip.test.ts` — 往復検証（3-1）
- `src/domain/integration/ifc/units.ts` — IFC単位解決（3-3）
- `src/schemas/structuralAnalysis.schema.json` — 構造計算JSONスキーマ（3-6）
- `src/domain/migration/` — スキーマ移行（3-7）

---

*本提案は調査時点のコードに基づく。実装着手時は各該当ファイルの最新状態を再確認すること。*
